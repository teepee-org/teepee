# Demo Workspaces

`hn-workspace/` is the recommended sandbox for a public Teepee demo.

It is intentionally isolated from the main repo and includes:
- a disposable mini project
- a local `.teepee/config.yaml`
- prompt ideas for a live demo

Start from there:

```bash
cd demo/hn-workspace
npx teepee-cli start
```
